import 'package:flutter/material.dart';

class SchoolClass extends StatelessWidget {
  const SchoolClass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("This is from School Class"),
      ),
    );
  }
}
